export default class Config {
    static maxBeats: number = 100;
    static maxTracks: number = 50;
    static maxBars: number = 100;
    static maxBpm: number = 300;

    static sampleRates = [44100, 48000];
}
