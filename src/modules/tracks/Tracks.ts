import { html, LitElement, type TemplateResult } from "lit";

export default class Tracks extends LitElement {
    protected render(): TemplateResult {
        return html`
            <card-component card-width="1000px"> bla bla </card-component>
        `;
    }
}
